import {useState} from "react";
import './Node.css';


const Node = ({row,col}) =>{ // the row and col props are received from higher up
    return <div className="node"></div> // a node simply shows as a 20/20 block
}

export default Node;